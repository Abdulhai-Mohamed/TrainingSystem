{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "ConnectionStrings": {
        "SqlConnection": "server=LAPTOP-G691MIDI\\SQLEXPRESS;database=TrainingSystemDB;Trusted_Connection=true;Encrypt = false;"
    },
    "JWtSettings": {
        "validIssuer": "AbdoApi", //name of the entity that issued the JWT, In this case, validIssuer is set to "AbdoApi", which means that any JWTs issued by "AbdoApi" will be considered valid
        "validAudience": "https://localhost:7131", //specifies the intended recipient of the JWT ,In this case validAudience is set to "https://localhost:7131", which means that any JWTs intended for "https://localhost:7131" will be considered valid.
        "expires": 5 //expires specifies the time (in minutes) after which a JWT will expire. In this case, it is set to 5, which means that any JWTs issued will expire after 5 minutes.
    },
  "AllowedHosts": "*"
}
